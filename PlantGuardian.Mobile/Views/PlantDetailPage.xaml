<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:PlantGuardian.Mobile.ViewModels"
             x:Class="PlantGuardian.Mobile.Views.PlantDetailPage"
             Title="{Binding Plant.Name}">

    <ScrollView>
        <VerticalStackLayout Spacing="20" Padding="20">
            <Image Source="{Binding Plant.ImageUrl}" HeightRequest="250" Aspect="AspectFill" />
            
            <Label Text="{Binding Plant.Name}" FontSize="28" FontAttributes="Bold" />
            <Label Text="{Binding Plant.Species}" FontSize="18" TextColor="Gray" />
            <Label Text="{Binding Plant.SoilType, StringFormat='Đất: {0}'}" FontSize="16" />
            <Label Text="{Binding Plant.PlantType, StringFormat='Loại cây: {0}'}" FontSize="16" />

            <!-- Bean Detail Button - only visible for bean plant types -->
            <Button Text="🫘 Xem Chi Tiết Chăm Sóc Đậu"
                    Command="{Binding ViewBeanDetailsCommand}"
                    IsVisible="{Binding IsBean}"
                    BackgroundColor="#4CAF50" TextColor="White"
                    CornerRadius="10" HeightRequest="50" FontSize="15" FontAttributes="Bold"/>

            <Button Text="Xóa Cây" BackgroundColor="Red" TextColor="White"
                    Command="{Binding DeletePlantCommand}"
                    CornerRadius="10" HeightRequest="44"/>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
